import { Typography, Container } from '@mui/material';

export default function Dashboard() {
  return (
    <Container>
      <Typography variant="h4" mt={3}>Dashboard</Typography>
      <Typography>Welcome to Garage Management System</Typography>
    </Container>
  );
}
